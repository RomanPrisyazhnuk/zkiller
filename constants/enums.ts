export enum gender {
  male ='male',
  female = 'female',
  both = 'female both male',
}

export enum army {
  soldier = 'soldier',
  auto = 'auto',
  tank = 'tank',
  bpla = 'bpla',
  copter = 'copter',
  plane = 'plane',
  ship = 'ship',
}

export const categories = {};
